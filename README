Переписал класс ThreadLocal
Реализованы методы get, set и remove
Написал 2 "теста":
1. TestConcurrentAccessToThreadLocal - к одному tl обращаются несколько тредов и инкрементируют счетчик и сравивают с предыдущим значением (должно быть на 1 меньше иначе проблема с параллельным доступом)
2. TestMapCleanUp - создаются несколько тредов и записывют что-то в TL, затем "умирают". ожидается, что после этого в связанные с ними TL переменные будут удалены

